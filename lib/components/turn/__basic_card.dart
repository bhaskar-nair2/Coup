import 'package:flutter/material.dart';

class BasicActionsCard extends StatelessWidget {
  const BasicActionsCard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
